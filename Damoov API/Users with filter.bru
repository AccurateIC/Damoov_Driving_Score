meta {
  name: Users with filter
  type: http
  seq: 12
}

get {
  url: {{BASE_URL}}/users?filter=last_2_months
  body: none
  auth: inherit
}

params:query {
  filter: last_2_months
}

settings {
  encodeUrl: true
}
