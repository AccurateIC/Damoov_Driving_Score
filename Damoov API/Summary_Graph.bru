meta {
  name: Summary_Graph
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/summary_graph
  body: json
  auth: inherit
}

body:json {
  { "filter_val": "last_2_months", "metric": "Trips" }
}

settings {
  encodeUrl: true
}
